#include "fixed_point.h"

/*
 * This library currently does not have any functions to implement; all
 * functionality is contained in the header macros.  It may be expanded in the
 * future with functions for more math operations if the application requires
 * them, at which point this comment should be removed.
 */
